import Logo from "../../UI/Logo";
import Hamburger from "../../UI/Hamburger";

const MobileNav = () => {
  return (
    <div>
      <div>
        <Logo />
        <Hamburger />
      </div>
    </div>
  );
};

export default MobileNav;
